import { inter } from "../../fonts";

export default function Transactions() {
  return (
    <main className={`${inter.className} text-dashboard-color`}>
      <h1> Transactions </h1>
    </main>
  );
}
